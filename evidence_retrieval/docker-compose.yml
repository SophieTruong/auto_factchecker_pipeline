services:
  # Semantic Search Service
  evidence_retrieval:
    build: .
    env_file:
      - .env
    volumes:
      - ${HF_HOME}:/app/sentence-transformer-model
    networks:
      - backend
    depends_on:
      - rabbitmq
      - milvus_standalone
networks:
  backend:
    driver: bridge
