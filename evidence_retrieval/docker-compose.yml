services:
  # Semantic Search Service
  evidence_retrieval:
    build: .
    networks:
      - backend
    volumes:
      - ${HF_HOME}:/app/sentence-transformer-model
networks:
  backend:
    driver: bridge
